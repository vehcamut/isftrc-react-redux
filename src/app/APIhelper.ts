// eslint-disable-next-line import/prefer-default-export
export const fff = async (callback: object) => {
  console.log(callback);
  // try {
  //   await callback();
  //   navigate('/auth');
  // } catch (e) {
  //   dispatch(setLoginHelper('Неправильный логин или пароль'));
  // }
};
